```mermaid 

sequenceDiagram
    actor 교수사용자 as 교수 (User)
    participant UI as 입력_화면.jsp (Boundary)
    participant Prof as 담당교수 (Entity: 교수)
    participant Score as 과목성적 (Entity: 성적)

    교수사용자->>UI: 입력버튼클릭(교수id, 학생id, 점수들)
    activate UI
    
    UI->>Prof: 교수체크(입력id)
    activate Prof
    Prof-->>UI: boolean (true/false)
    deactivate Prof

    alt 인증 성공 (true)
        UI->>Score: 성적입력(학생id, 자바, DB, 보안)
        activate Score
        Score->>Score: 총점계산()
        Score->>Score: 평균계산()
        Score->>Score: 학점결정()
        Score-->>UI: 입력 완료 응답
        deactivate Score
        UI-->>교수사용자: "성적 입력이 완료되었습니다" 출력
    else 인증 실패 (false)
        UI-->>교수사용자: "교수 인증 실패" 에러 메시지 출력
    end
    
    deactivate UI