```mermaid 
sequenceDiagram
    actor 교수사용자 as 교수 (User)
    participant UI as 조회_화면.jsp (Boundary)
    participant Prof as 담당교수 (Entity: 교수)
    participant Score as 과목성적 (Entity: 성적)

    교수사용자->>UI: 학점조회 요청(교수id)
    activate UI
    
    UI->>Prof: 교수체크(요청id)
    activate Prof
    Prof-->>UI: boolean (true/false)
    deactivate Prof

    alt 인증 성공 (true)
        UI->>Score: 학점조회()
        activate Score
        Note right of Score: 저장된 총점, 평균, 학점 데이터를 문자열로 포맷팅
        Score-->>UI: String (학점 정보 문자열)
        deactivate Score
        UI-->>교수사용자: 조회 결과 텍스트 출력
    else 인증 실패 (false)
        UI-->>교수사용자: "조회 권한이 없습니다" 에러 메시지 출력
    end
    
    deactivate UI